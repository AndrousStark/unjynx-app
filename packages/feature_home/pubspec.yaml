name: feature_home
description: UNJYNX Home Hub feature plugin - command center with progress, content, and tasks.
version: 0.1.0
publish_to: none

resolution: workspace

environment:
  sdk: ^3.9.0
  flutter: ">=3.27.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_riverpod: ^3.3.0
  go_router: ^16.0.0
  just_audio: ^0.9.40
  path_provider: ^2.1.0
  screenshot: ^3.0.0
  share_plus: ^10.1.4
  unjynx_core:
    path: ../core

dev_dependencies:
  flutter_test:
    sdk: flutter
  very_good_analysis: ^7.0.0
