name: service_database
description: UNJYNX database service - Drift adapter implementing DatabasePort
version: 0.1.0
publish_to: none

resolution: workspace

environment:
  sdk: ^3.9.0
  flutter: ">=3.27.0"

dependencies:
  flutter:
    sdk: flutter
  drift: ^2.32.0
  sqlite3_flutter_libs: ^0.5.0
  path_provider: ^2.1.0
  path: ^1.9.0

  unjynx_core:
    path: ../core

dev_dependencies:
  flutter_test:
    sdk: flutter
  build_runner: ^2.12.2
  drift_dev: ^2.32.0
  very_good_analysis: ^7.0.0
